{
  "name": "fb_kr3",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run sass\"",
    "server": "live-server --no-css-inject --wait=100",
    "sass": "sass --watch styles/styles.scss:styles/styles.css",
    "certificate": "mkcert -install && mkcert localhost 127.0.0.1 ::1",
    "https": "http-server --ssl --cert localhost+2.pem --key localhost+2-key.pem -p 3000"
  }
}
